section class="container">
  <h3 class="gallery-name">{{blog.title}}</h3>

  <section class="flex-row justify-center">
    {{#each blog.comment as |comment|}}
    {{> comment-details}}
    {{/each}}
  </section>
</section>